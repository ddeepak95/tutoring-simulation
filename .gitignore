.env
.venv/
__pycache__/
runs/extra
uv.lock
# annotation tool local DB
annotations.sqlite3

# generated run set
runs/convolearn/run_set_07081515/*
!runs/convolearn/run_set_07081515/conversations.jsonl

# generated dialogic runs and translations

# sampled target turns
runs/target-mono/
runs/target-test/
runs/targets/*/

# intermediate for the tutor comparison page; build.py only writes it with --keep-json.
# The built page itself IS tracked, so the html can be opened without running anything.
viz/*/data.json
*.py[cod]
# Collected data and browser profiles are also ignored by explanation/.gitignore.
consolidated-study/
# Oversized local reference PDFs (not stored in Git).
/textbooks/igcse/Cambridge IGCSE™ Chemistry Coursebook with Digital Access (2 Years) (Cambridge International IGCSE) (Harwood, Richard, Lodge, Ian, Millington, Chris) (z-lib.org).pdf
/textbooks/openstax/chemistry-2e_-_WEB.pdf
