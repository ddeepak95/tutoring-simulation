# Optional: configure only the providers used by your collection/analysis commands.
OPENAI_API_KEY=
GEMINI_API_KEY=
SERPAPI_API_KEY=
ANTHROPIC_API_KEY=
# The SerpAPI collector also accepts the existing SERP_KEY variable.
# Vertex models use Google Application Default Credentials where configured.
# Keep actual credentials in .env or environment variables, never in this template.
